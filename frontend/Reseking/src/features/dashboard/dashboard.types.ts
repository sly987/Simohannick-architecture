export type DashboardStats = {
  uniqueUsers: number;
  averageOccupancyRate: number;
  noShowRate: number;
  electricSpotRatio: number;
};

export type DateRange = {
  from: string;
  to: string;
};
